version: "3"

services:
  backend:
    container_name: elants_backend
    build: 
      dockerfile: Dockerfile
      args:
      - ELANTS_CONFIG_FILE_PATH=./configs/main_config.json
    # depends_on:
    #  - db
  # db:
  testing:
    container_name: elants_backend_testing
    build:
      context: .
      dockerfile: ./testing/Dockerfile
  # linting:
