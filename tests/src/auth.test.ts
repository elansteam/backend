import {describe, test} from "@jest/globals";
import { cleanup } from "./helpers/scripts";


describe("Auth", async() => {
  cleanup();

  test("Signup test (only ELAN)", async () => {

  });

  test("Signin", async () => {

  });
});