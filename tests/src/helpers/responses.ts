namespace RS {
  export interface AuthSignin {
    accessToken: string,
    refreshToken: string
  }

  export interface AuthSignup {
    accessToken: string,
    refreshToken: string
  }
}

export default RS;
